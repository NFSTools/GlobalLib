namespace GlobalLib.Utils
{
    public static class Formatter
    {
        public static string ToString(string value, string format)
        {
            // say format is "Color[{X}]"
            return null;
        }
    }
}